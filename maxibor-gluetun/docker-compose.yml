services:
  server:
    image: qmcgaw/gluetun:v3.41.2@sha256:280809bc6900ed06d6529ad246499103efb31fd80c6327cbfaf90de0b17c3a99
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      - "9091:9091"
      - "51413:51413"
      - "51413:51413/udp"
      - "8000:8000"
    volumes:
      - ${APP_DATA_DIR}/data:/gluetun
    environment:
      # CyberGhost VPN Configuration
      - VPN_SERVICE_PROVIDER=cyberghost
      - VPN_TYPE=openvpn
      - OPENVPN_USER=${CYBERGHOST_USERNAME}
      - OPENVPN_PASSWORD=${CYBERGHOST_PASSWORD}
      - OPENVPN_CERT=${OPENVPN_CERT}
      - OPENVPN_KEY=${OPENVPN_KEY}
      - SERVER_COUNTRIES=${SERVER_COUNTRIES}
      # Enable Control server
      - HTTP_CONTROL_SERVER_ADDRESS=:8000  # 👈 fixed: = not :
      - HTTP_CONTROL_SERVER_LOG=on
      - TZ=Europe/Amsterdam
    restart: unless-stopped
    stop_grace_period: 1m